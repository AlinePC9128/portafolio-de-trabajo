const $=s=>document.querySelector(s);$('#send').addEventListener('click',()=>{const payload={name:$('#name').value,email:$('#email').value,company:$('#company').value,source:$('#source').value,consent:$('#consent').checked};const valid=payload.name.trim().length>=2&&payload.email.includes('@')&&payload.company.trim().length>=2&&payload.consent;$('#status').textContent=valid?'201 created':'422 validation error';$('#status').style.color=valid?'#2e9360':'#c73863';$('#response').textContent=JSON.stringify(valid?{data:{...payload,id:'lead_demo_001',status:'new'},meta:{requestId:'req_demo_001'}}:{error:'VALIDATION_ERROR',details:{message:'Revisa los campos y el consentimiento.'}},null,2)});
